# Agora Public API Plugin

This plugin contains reviewed agent skills for Agora's public API-key workflow. Skills are generated from the checked-in public OpenAPI contract and then reviewed before release.

Current skill:

- `agora-lead-upload-api`: token exchange plus lead upload examples for curl, Restish, and generated SDK guidance.
